/**
 * (citizen) route group layout — pass-through only.
 *
 * CitizenLayout is provided by app/citizen/layout.tsx.
 * This group exists for organisational purposes only.
 */
export default function CitizenRouteGroupLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
