import CitizenLayout from "@/components/layout/CitizenLayout";

export default function CitizenGroupLayout({ children }: { children: React.ReactNode }) {
  return <CitizenLayout>{children}</CitizenLayout>;
}
